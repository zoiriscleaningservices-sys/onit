name: Zoiris Auto Blog Agent

on:
  schedule:
    - cron: "*/5 * * * *"  # Every 5 minutes (GitHub's minimum allowed frequency)
  workflow_dispatch:  # Allows manual triggering from GitHub UI

jobs:
  post-blog:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'  # Updated to a more recent LTS version (as of late 2025)

      - name: Install dependencies
        run: npm install node-fetch  # Or better: use npm ci if you have a package-lock.json

      - name: Run AI Agent
        run: node agent.js
        env:
          GEMINI_API_KEY: ${{ AIzaSyCs0ggrX5CAERGlMcZsGTt8zrpBSnNRmTk }}
          SUPABASE_URL: ${{ https://dwxbzltxsdeshmmtcycv.supabase.co }}
          SUPABASE_SERVICE_KEY: ${{ eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6ImR3eGJ6bHR4c2Rlc2htbXRjeWN2Iiwicm9sZSI6InNlcnZpY2Vfcm9sZSIsImlhdCI6MTc2Njk0ODQzOCwiZXhwIjoyMDgyNTI0NDM4fQ.ylRVbzU48IQePs-iKGchagxMq3t7iWy4eT2JdkJkKT8 }}
